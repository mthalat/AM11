services:
    - type: web
      name: fridge-genius
      runtime: node
      buildCommand: npm install
      startCommand: node server.js
      envVars:
        - key: GOOGLE_API_KEY
          sync: false
  